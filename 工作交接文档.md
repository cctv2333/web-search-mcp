# 工作交接文档 —— Web 搜索与阅读 MCP（web-search-mcp）

> 生成：2026-09-05　用途：给接手的人/未来的 AI 看。核心：做了什么、踩了什么坑、还有什么问题、隐患在哪、
> 每份文件是干嘛的。
> 主目录：`F:\DeepSeek-Harness\config\examples\web-search-mcp`（副本：`F:\MCP 文件副本\web-search-mcp`，内容一致、不含 .venv）
> 运行环境：Windows + Python 3.12（venv）+ FastMCP v1（mcp 1.29.x）+ requests + trafilatura 2.2

---

## 一、这是干什么的（一句话）
一个**本地 MCP 服务器**，给 DeepSeek Harness / 任意 MCP 客户端提供"网页搜索（免 Key Bing 默认 + 可选 Tavily/博查）、结果去毒去重、网页全文阅读（含多页 & JS 渲染页兜底）"。工具名前缀 `ws_*`，接入后以 `mcp__websearch__ws_*` 出现。

## 二、功能与能力
| 能力 | 说明 |
|---|---|
| `ws_search` | 默认免 Key Bing；可选 tavily/bocha/auto。管线：类型打标(参考类置底/可剔除) → 去重(去跟踪参数) → 广告/毒站剔除 → TopN 存活核验(404剔/403注/标题不符标注) |
| `ws_read` | **四级读取兜底**：① trafilatura 静态正文提取 → ② 同站 RSS/Atom → ③ 本机浏览器无头渲染(`--dump-dom`) → ④ 仍失败如实报错。多页翻页(`WS_PAGING_MODE`)；SSRF/体积/超时限制 |
| `ws_providers` | 搜索源 + 阅读能力自述（浏览器渲染可用性、视觉读图提示）+ 抓取配置 |
| `ws_ping` | 连通自检 |

抓取纪律（刻意为之）：robots 尊重、限速、如实 UA、无 Cookie/JS（静态通道）、SSRF 防护、**绝不绕过验证码/登录墙/付费墙**；被拦如实报并给替代路径（此文即用户强调的"实事求是/不伪造"）。

## 三、文件清单与职责
```
web-search-mcp/
├─ server.py                入口：FastMCP(instructions=…) + 注册 ws_ping/ws_providers/ws_search/ws_read，stdio
├─ wsweb/
│  ├─ __init__.py           包说明
│  ├─ config.py             所有环境变量/上限/模式（密钥只在此读取，绝不下沉）
│  ├─ errors.py             结构化错误 WebError 及子类（含 code，工具层据此不崩溃）
│  ├─ models.py             SearchResult(kind/note…) / ReadResult 数据模型 + as_text
│  ├─ urlutil.py            URL 规范化、去跟踪参数、去重键、Bing ck/a 跳转 base64 解码、域名后缀匹配
│  ├─ safety.py             SSRF 防护(check_public_http)、robots(RobotsGate)、限速(PoliteFetcher)、_smart_decode 智能解码
│  ├─ filtering.py          类型打标(classify)、去重、广告/毒站启发式、drop_kind、pipeline
│  ├─ providers.py          搜索源抽象：bing(RSS主/HTML兜底)、tavily、bocha；resolve/list_provider_status
│  ├─ reader.py             正文提取、翻页(next 探测/数字页码/_next_candidates)、RSS 兜底(_feed_links/_feed_to_lines)、
│  │                        浏览器渲染兜底、_visible_text(轻量可见文本第二通道)
│  ├─ browser.py            本机 Edge/Chrome 无头 --dump-dom 渲染（SSRF+robots 门禁，失败如实报）
│  └─ service.py            编排 run_search(过滤/核验/报告) / run_read
├─ test_self.py             S1-S6 自测（离线单测 + 经 stdio 起 server 冒烟）；加过 SSRF/类型/RSS/浏览器模式等单测
├─ demo_live.py             S8 实测演示：真实 ws_search + ws_read
├─ requirements.txt         依赖：mcp>=1.2,<2 / requests / trafilatura（**必须纯 ASCII**，见坑①）
├─ .env.example             全部可配置项（密钥只走环境变量）
├─ .gitignore               排除 .venv/__pycache__/.env/.tmp
├─ setup.bat                一键：建 .venv + 装依赖 + 跑自测
├─ dsh-patch.example.yml    DSH 接入补丁（--patch 或并入 cordis.patch.yml）
├─ 重启后验证指引.md         接入后的验证步骤（含启用 Tavily/博查的钥匙说明）
├─ README.md                使用/接入/合规文档
├─ PLAN.md                  方案/事实/方针/计划 + 修复记录
└─ 工作交接文档.md           本文件
```

## 四、做了什么事（里程碑）
1. S1-S8：骨架 → Bing 免Key → 过滤去毒核验 → Tavily/博查可插拔 → 单页正文 → 多页翻页 → 接入 cordis.patch.yml(热生效) → 本会话实测。
2. 接入即修（真机抓 bing/DNS 实测）：
   - SSRF 首版把域名全拦（`_is_public_ip` 对域名也返回 False）→ 先判字面 IP 再 DNS 复查。
   - requests 无 charset 默认 ISO-8859-1 解码致中文乱码 → `_smart_decode`（头 charset→meta→utf-8→gb18030）。
   - 多页 rel=next 把"下一篇公告"当续页 → `WS_PAGING_MODE`（默认 page=只信数字页码）。
3. 外部压力测试（另一位 AI 做的 20 次真实调度）暴露：
   - 词典/百科/音乐污染新闻查询 → 结果类型打标 `reference`（标注置底 / `exclude_kind=reference` 剔除），实测 6/8 条被正确识别。
   - 主流新闻站读不到 → RSS 兜底 + 明确错误成因与替代路径。
   - IPv6 SSRF 误拦 aljazeera（v4 公网 + Teredo 前缀 v6 混合）→ 改为"全部非公网才拦，混合放行"，补单测。
4. 浏览器渲染兜底（用户提议）：静态+RSS 都失败 → 本机 Edge/Chrome 无头渲染 JS 页。修一个真 bug：trafilatura 对模块化布局整页过滤返回 0 字符 → 新增 `_visible_text` 第二通道。能力自感知：ws_providers/instructions 明示"支持图像的模型可截图读图，否则走渲染文本"。

## 五、踩过的坑（现象 → 根因 → 修复）
| # | 坑 | 根因 | 修复/现状 |
|---|---|---|---|
| 1 | pip 报 UnicodeDecodeError 读不了 requirements.txt | 里含中文注释，Windows 下 pip 按 GBK 读 | 改成**纯 ASCII** 注释；以后加注释务必 ASCII |
| 2 | pip 下载近乎停滞（PyPI 二进制） | 官方源二进制通道慢/被限 | 换清华镜像安装成功 |
| 3 | 自测报 `list_tools` 返回元组无法 `.name` | mcp v1 返回形态兼容问题 | `_tools_list()` 兼容 tools/元组/裸 list |
| 4 | 控制台中文乱码（`✓` 崩 / GBK 显示） | Python 默认控制台编码 cp936 | `sys.stdout.reconfigure(utf-8)` |
| 5 | 所有域名被 SSRF 拦 | `_is_public_ip` 对域名返回 False 走字面分支 | 先字面 IP，域名走 DNS 复查（还原 127.0.0.1 仍拦） |
| 6 | 正文中文乱码 `ä»å¤©` | requests 无 charset 默认 ISO-8859-1 解码 UTF-8 字节 | `_smart_decode` 多级解码 |
| 7 | 把 Docusaurus"下一篇"当续页连读 | rel=next 语义是相邻文档非本页分页 | `WS_PAGING_MODE=page` 默认只信数字页码 |
| 8 | aljazeera 被 SSRF 拦 | v4 公网 + 2001::(Teredo 前缀)v6 混合解析，"任一非公网即拦" | 全部非公网才拦、混合放行（含单测） |
| 9 | 搜"最近国际新闻"全是歌曲/词典 | 通用索引对百科词典权重高、无新闻垂直 | 类型打标 reference + 默认置底 + 可剔除；引擎根源无法本地解决 |
| 10 | 浏览器渲染拿到 218KB HTML 但 0 字符 | trafilatura 对模块化布局整页过滤 | `_visible_text` 第二通道（实测 mcpcn 读出 2343 字符） |
| 11 | 热重载不完全可靠 | harness live watch 偶发不响应后续配置改动 | 改代码后若 watch 没换进程，重启 GUI 加载；重要改动以重启为准 |
| 12 | example.com 被 SSRF 拦（非 bug） | 国内 DNS 常污染 example.com 为非公网 | 系防护正确，非缺陷 |

## 六、已知问题与隐患（如实，按影响排序）
1. **Bing RSS 授权**：仅限"个人非商业、低流量展示"；其它用途需微软书面许可。本工具默认请求 Bing，开源/商用前必须换数据源或让用户自负（已在 README 声明）。
2. **无新闻垂直/地域语言参数**：Bing 免 Key 不做"新闻类"定向，结果相关性靠通用索引 → 新闻/时效类查询可能仍偏；根治需 Tavily/博查 Key（未配未实测）。
3. **参考类识别是启发式**（域名单 urlutil 内可扩展），可能误标/漏标；只做标注不静默剔除，误伤小。
4. **网络可达性差异**：BBC/DW/NHK 等出口超时；AP/以色列时报 403；部分站验证码；这些是网络/反爬层，非工具 bug，且被拦时不绕。
5. **浏览器渲染依赖本机浏览器**（Edge/Chrome），无则 `WS_BROWSER_PATH` 指定；较慢(几秒~十几秒)；无头 UA 特征可能被个别站拦；仍守 robots/不绕墙。
6. **视觉读图未启用**：当前 GUI 模型 `deepseek-v4-flash` 不支持图像输入；要"截图→读图"须把本会话模型换成 vision 版（如 `deepseek-v4-flash-vision-exp`）。工具已在文字层面提示该能力。
7. **热重载时好时坏** + GUI 工具选择器/斜杠菜单**不枚举 MCP 工具**（工具只注入会话模型可调用集，实际能用但界面看不到入口）。这是 Harness 前端呈现缺口，非功能缺失。
8. **密钥透传**：harness 会剥子进程环境的 KEY/TOKEN/DSH_* 变量，故 Tavily/博查 Key 需**在启动 dsh 前**导出，并经 `cordis.patch.yml` 的 `env: {KEY: !!js process.env.KEY}` 显式透传；当前未配 Key，相关源未实测。
9. **路径/机器相关**：README/PLAN/示例里的 `F:\DeepSeek-Harness`、`C:\Users\cctv2\.dsh`、`.venv` 绝对路径是**本机示例**，换机器/开源需替换；副本不含 `.venv`（用 `setup.bat` 重建）。
10. **多版本混淆的隐患**：本机 `F:\DeepSeek-Harness` 只有一份（浅克隆，HEAD 0.1.3-alpha.1，仅供查阅）；**实际运行**的是全局 npm `@deepseek-ai/dsh v0.1.2-rc.1`。别再新建小写 `F:\deepseek-harness`（同一物理目录，大小写不敏感，会误以为有两份）。
11. **免费源稳定性**：免 Key 直连 Bing 可能被风控(429/验证)；有 HTML 兜底；必要时含 `curl_cffi`（可选依赖，未启用）。
12. **SSRF 混合策略为权衡**：放行"公网+非公网混合解析"可能允许含内网的解析通过——已注释说明，依赖 HTTPS + 目标合法性 + 信任模型；如需更严可回退"任一非公网即拦"。

## 七、工作方法与约定（接手者必读）
- 用户方针：**先提设想 → 实事求是查证 → 定方针 → 定计划 → 按计划实施**；重大改动先出方案文档经确认再动手。
- 铁律：**AI 不执行任何写 git 历史/写远端操作**；**密钥绝不落盘**只走环境变量；改码后跑 `test_self.py` 全绿 + 真机验证；结论沿调用链核实到源头。
- 每次改动一步 = 同步副本 + 在 PLAN.md 修复记录里补一条"发现→修复→实测"。

## 八、后续建议（可选）
1. 补配 Tavily/博查 Key 实测付费源（根治新闻语义）。
2. `ws_read` 加"浏览器渲染"选项做成显式模式/预置可读新闻源清单（UN News/新华英文等静态站）。
3. 视觉流：GUI 切 vision 模型后，补一个"截图→读图"的 ws 工具或指引。
4. 开源：加 LICENSE（建议 MIT）+ README 加通用部署/开源章节 + 把本机路径改成占位符。
5. 若长期用，增加结果缓存（RoleplayChat 实现是 5min TTL/max256，可抄）。
