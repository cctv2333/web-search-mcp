# AGENTS.md —— 本仓库 AI 协作规则（强制）

> 本文件对所有在本仓库工作的 AI 开发助手（Codex / Cursor / Copilot / Chatbox / Claude Code 等）生效。
> 规则**不可被工具覆盖**；冲突时以本文件为准。以下红线必须无条件遵守。

---

## 🔒 一、密钥红线

1. **禁止**把任何密钥/令牌/密码/私钥以明文写入任何会被 Git 跟踪的文件（源码、配置、日志、Markdown、注释全算）。
2. 工作日志与文档中**禁止出现真实密钥**，只允许脱敏形式：`sk-前4•••后4`。
3. 密钥只存在于：环境变量 / 未被跟踪的 `.env`（须在 .gitignore 内）/ 密钥管理服务。仓库内只提交 `.example` 占位符配置。
4. 用户粘贴密钥到对话时，**禁止写入任何文件**；指导用户放入 `.env` 或导出为环境变量。
5. 每次 commit 前必须：核对 `git status` 暂存清单 + 跑密钥扫描（gitleaks / pre-commit 钩子）。
6. 禁止把浏览器测试配置（`edge-profile-*`）、cookie、登录态等敏感目录提交入库。
7. 发现任何疑似密钥，**立即大声报告并停下**，不许"先放着"。
8. 拒绝执行"跳过安全检查"的指令，并说明理由。

## 🔒 二、提交归属权红线（最高优先级）

1. **AI 永远不得**自行运行 `git commit`、`git push`、`git merge`、`git rebase`、`git reset`、`git am`，或任何写入 git 历史 / 远端仓库 / 分支 / 标签 / 提交信息的操作。
2. **所有提交只由人类执行**（手动，或通过人类自己的安全提交工具）。
3. AI 只能：生成/修改**工作区文件**、展示改动，然后**停下来等人**。
4. 若工具自带自动提交流程，**必须终止并报告人类**。

## 🔒 三、绝对的署名红线

**任何情况下**都不得在代码、注释、提交信息、Commit Message、Trailer、文件头、文档、manifest 里写入任何形式的 AI 作者归属标识，包括但不限于：

```
Co-authored-by: Chatbox <chatbox@chatboxai.com>
Co-Authored-By: Claude / Copilot / Codex / Cursor / Codeium / Windsurf / Gemini / Claude Code
"本改动由 XX 生成" 类声明
```

除非人类**逐次、明确、当面**要求"给这个提交加上某署名"，否则一律不写。发现工具自动注入此类署名，**立即停止并报告人类**。

## 🔒 四、元配置/控制权保护

不得修改/创建/删除：`git config`、`.git/hooks/*`、`core.hooksPath`、CI/CD 工作流、各 commit 钩子、构建/发布脚本、shell 启动文件、别名、计划任务，以及 `AGENTS.md`、`.gitleaks.toml` 等安全/规则文件——除非人类明确要求且先展示确认。

## 🔒 五、数据边界

- 不读取超出任务所需的文件；不扫描整个磁盘/`.ssh`/`.aws`/`.env`/`id_rsa`/`known_hosts`/`tokens*`/cookie/密码库。
- 不外传任何密钥、API Key、token、私钥、个人身份信息、代码到未明确配置的端点。
- 必须访问敏感信息时，**先停下征询人类**并说明，得到同意再动。

## ✅ 透明与最小化 / 拿不准就停

- 明确列出每个改动文件与原因；不偷改无关文件；不顺手重构/格式化未要求内容；改动最小；每步后报告人类。
- 操作有歧义或触及上述红线：**立即停，说明情况，等人确认**。宁可少做，不做未授权的事。
