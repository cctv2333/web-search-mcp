# 重启后验证指引（最后一步：让模型侧出现 mcp__websearch__ws_* 工具）

> 背景：MCP 服务器已接入并热加载成功（harness 已拉起 server 子进程、清洗环境下握手自测通过），
> 但**本会话的模型工具快照未刷新**，需 GUI 重启/新会话后模型才能直接调用这些工具。

## 步骤（约 1 分钟）

1. **重启 DeepSeek Harness GUI**：
   - 回到启动它的窗口（start-dsh.cmd / dsh web），Ctrl+C 停掉后重新运行；
   或直接新开一个浏览器会话（地址仍为 http://127.0.0.1:3080）。
   - 也可先确认：GUI 的 设置 → 插件/工具 或工具列表里应能看到 `websearch` 服务器与 4 个 ws_* 工具。

2. **在对话里直接点名调用**，例如：
   > 用 mcp__websearch__ws_search 搜「DeepSeek V4 最新发布」，然后 mcp__websearch__ws_read 读第一条的正文。
   或在 GUI 工具选择器里直接选中 `mcp__websearch__ws_search`。

3. **预期结果**：返回 8 条左右 Bing 有机结果（中文、带时间/来源、剔除项会列出原因），
   阅读返回干净正文（多页文章自动翻页拼接，页数与字符数可见）。

## 可选：启用 Tavily / 博查
在**启动 dsh 之前**的同一 shell 里导出密钥（Harness 会剥掉子进程环境里的 KEY/TOKEN 变量，只能靠配置透传）：
```powershell
$env:TAVILY_API_KEY = "tvly-..."
$env:BOCHA_API_KEY  = "sk-..."
```
之后 `ws_providers` 会显示对应源"可用"。

## 排查看这里
- 服务器日志：.venv 里 `python server.py` 的 stderr（harness 会转发到它的日志/窗口）。
- 快速自检（无需 GUI）：`.venv\Scripts\python.exe test_self.py`（全绿=S1-S6 OK）；
  `.venv\Scripts\python.exe demo_live.py "查询词"`（真机搜索+阅读演示）。
